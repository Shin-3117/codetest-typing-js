const starterCode = `globalThis.kruskalMST = function kruskalMST(vertexCount, edges) {
  // vertexCount: number (vertices are 0..vertexCount-1)
  // edges: Array<[number, number, number]> where [u, v, weight]
  // return: { totalWeight: number, mstEdges: Array<[number, number, number]> }
  // Requirement: use Union-Find with union by rank

  // TODO
};`;

export default starterCode;
