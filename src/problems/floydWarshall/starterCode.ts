const starterCode = `globalThis.floydWarshall = function floydWarshall(graph) {
  // graph: number[][] (인접 행렬, 자기 자신 0, 직접 연결 없는 경우 Infinity)
  // return: number[][] (모든 쌍 최단 거리 행렬)

  // TODO
};`;

export default starterCode;
