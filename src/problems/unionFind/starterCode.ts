const starterCode = `globalThis.UnionFind = class UnionFind {
  constructor(n) {
    // parent[i] = i initially
    // rank[i] = 0 initially
  }

  find(x) {
    // path compression
    // TODO
  }

  union(a, b) {
    // union by rank
    // return true if merged, false if already in the same set
    // TODO
  }
};`;

export default starterCode;
