const starterCode = `globalThis.permute = function permute(nums) {
  // nums: number[] with unique values
  // return: number[][] containing all permutations
  // Requirement: use backtracking (choose -> recurse -> undo)

  // TODO
};`;

export default starterCode;
