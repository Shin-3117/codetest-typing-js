const starterCode = `globalThis.binarySearch = function binarySearch(arr, target) {
  // arr: number[] (정렬된 배열)
  // target: number (찾을 값)
  // return: number (target의 인덱스 또는 없으면 -1)

  // TODO
};`;

export default starterCode;
