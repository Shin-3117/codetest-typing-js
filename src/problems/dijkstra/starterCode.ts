const starterCode = `globalThis.dijkstra = function dijkstra(graph, start) {
  // graph: Record<string, Array<[string, number]>>
  // start: string
  // return: Record<string, number> (shortest distance from start)
  // Rule: if start is missing in graph, return {}
  // Rule: keep unreachable nodes as Infinity

  // TODO
};`;

export default starterCode;
